# 조립 상자에 넣지 않을 것들.
# 서버 실행에 필요 없는 파일을 빼면 상자가 작아지고 조립이 빨라진다.

# ★ 절대 넣으면 안 되는 것: API 키. 키는 배포할 때 따로 심는다
.env
.env.*
!.env.example

# 서버 실행과 무관한 것들
tests/
scripts/
items/
outputs/
README.md

# 파이썬 임시 파일
__pycache__/
*.pyc
.pytest_cache/

# 가상환경 (상자 안에서 새로 설치하므로 불필요)
.venv/
venv/
