# GPU 서버(Colab · RunPod)에서 LoRA 학습을 돌리기 위한 목록.
# 여기 있는 것은 **실험용**이며, 채점 서버가 쓰는 assessment/requirements.txt 와 별개다.
# 채점 서버에는 이 무거운 것들을 절대 올리지 않는다(도커 이미지가 몇 GB 로 불어난다).
#
#   pip install -r requirements-lab.txt
#
# torch 는 여기 적지 않았다. Colab·RunPod 에는 그래픽카드에 맞는 torch 가
# 이미 깔려 있고, 여기서 다시 깔면 오히려 버전이 어긋나 GPU 를 못 쓰게 된다.
# 아무것도 없는 서버라면 https://pytorch.org 에서 그 서버의 CUDA 판을 먼저 깐다.

transformers>=4.44        # Whisper 모델 본체와 학습 도구
peft>=0.11                # LoRA (작은 판만 덧대어 배우는 방법)
datasets>=2.20            # 라벨 JSONL 을 학습용 표로 읽는다
accelerate>=0.33          # GPU 에 계산을 흩어 주는 도구 (Trainer 가 요구한다)
soundfile>=0.12           # wav 읽기
librosa>=0.10             # 촘촘함(샘플레이트)이 다를 때만 쓰는 변환기
jiwer>=3.0                # 글자·단어 오류율 계산 (평가용)

# ── RunPod LoRA 받아쓰기 추론 서버(lora_stt_server.py) ──────────────────────
# 학습해 둔 어댑터를 그래픽카드 서버에 띄워 채점 서버가 http 로 부르게 하는 서버.
# 위 transformers·peft·soundfile·librosa 에 더해 아래 셋이 필요하다.
fastapi>=0.110            # 추론 서버 뼈대(/health · /transcribe)
uvicorn>=0.29            # 그 서버를 실제로 띄우는 실행기
httpx>=0.27              # {url} 방식으로 음성을 받을 때 내려받는 도구

# 발음 표기 갈래(--variant mdd)를 만들 때만 필요하다.
# 윈도우에서는 설치가 안 되고 리눅스에서만 깔린다(까닭은 make_labels.py 참고).
g2pk>=0.9.4

# ── 점수 결합층 학습(train_score_model.py) ──────────────────────────────────
# 위의 것들과 달리 그래픽카드가 필요 없다. 자질 12개짜리 표 107줄을 다루는 계산이라
# 내 PC(.venv)에서 몇 초면 끝난다. 아래 둘만 있으면 된다.
xgboost>=2.0              # 손 가중치 대신 사람 점수로 결합 규칙을 배우는 나무 모델
scikit-learn>=1.4         # 화자 단위 겹 나누기(GroupKFold)와 등급 일치도(QWK)

# ── 세탁 탐지기 증인단 오디션(audition.py) ──────────────────────────────────
# 받아쓰기 모델 여러 종을 같은 시험지로 겨루게 하는 데만 쓴다. 채점 서버에는 필요 없다.
# 모델 파일이 10GB 가 넘으므로 받는 자리를 먼저 정하고 깐다:
#     set HF_HOME=D:\해커톤데이터\hf_cache
#
# faster-whisper 는 이미 깔려 있다(eval_ab.py 가 쓴다). 아래는 그 밖의 후보들.

funasr>=1.4               # SenseVoice-Small (직청형 후보) 를 부르는 도구
torchaudio                # funasr 가 소리를 읽을 때 요구한다

# OWSM v4 CTC (직청형 후보) — **윈도우에서는 그냥 깔리지 않는다.**
#   까닭: espnet 이 sentencepiece 를 0.2.0 딱 그 판으로 못 박아 두었는데,
#         그 판에는 파이썬 3.13 · 윈도우용 미리 만들어진 설치본이 없다.
#         pip 가 소스에서 빌드하려 들고 빌드 도구(cmake)가 없어 실패한다.
#   우회(8/7 에 이 PC 에서 쓴 방법 — 임시 조치다):
#         pip install --no-deps espnet espnet_model_zoo
#         pip install humanfriendly typeguard h5py g2p_en espnet_tts_frontend
#         (이미 깔린 sentencepiece 0.2.2 로 추론은 정상 동작 확인)
#   리눅스·파이썬 3.12 라면 그냥 `pip install espnet espnet_model_zoo` 로 끝난다.
espnet>=202511            # OWSM 추론 (위 우회 참고)
espnet_model_zoo>=0.1.7   # OWSM 가중치 내려받기

# Qwen3-ASR-1.7B (문맥형 후보) 는 따로 깔 것이 없다.
# 위에 적힌 transformers 5.x 에 이미 들어 있다(`Qwen3ASRForConditionalGeneration`).
# 주의: PyPI 의 `qwen-asr` 꾸러미는 transformers 4.57.6 을 못 박으므로 깔지 마라.
#       깔면 여기 있는 다른 스크립트들이 쓰는 transformers 5.x 가 뒤로 밀린다.
#
# Cohere Transcribe (문맥형 후보) 는 라이선스는 Apache-2.0 로 문제없지만
# 저장소가 '연락처 동의' 관문에 걸려 있어 HuggingFace 로그인 토큰이 있어야 받아진다.
# 토큰을 넣었다면 `huggingface-cli login` 뒤 audition.py 를 그대로 다시 돌리면 된다.

# ── 세탁 탐지기(launder_transcribe.py · launder_detect.py) ──────────────────
# 받아쓰기 쪽(launder_transcribe.py)은 위 오디션 목록 그대로면 된다 — 증인 4명이
# 전부 오디션 후보 중에서 뽑혔고 모델 코드를 audition.py 에서 그대로 가져다 쓴다.
#
# 판정 쪽(launder_detect.py)만 하나 더 필요하다. 그래픽카드도 인터넷도 안 쓴다.
kiwipiepy>=0.20           # 두 형태 중 어느 쪽이 표준 한국어인지 가리는 형태소 분석기
                          # (채점 자질에도 이미 쓰고 있어 이 PC 에는 깔려 있다)
