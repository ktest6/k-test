# K-TEST 실행 계획 v3 (~8/10)

> 2026-07-31 재편. **팀 회의에서 트랙 B(안전문서 → 문항 자동 생성)가 채택됐다.** 조건부(🔶) 해제.
> 상세 구상은 `IDEA.md`. 양보 불가 원칙: ① 응시자별 실시간 생성 금지(시험 전 생성 + 관리자 승인) ② 모든 생성물에 문서 인용 근거(없으면 자동 폐기).

## 전체 그림

```
[관리자] 안전문서 업로드 → AI가 문항+체크리스트 초안 생성(인용 검증) → 관리자 승인
                                                                    ↓
[응시자] 맞춤 시험 응시 → 기존 채점 파이프라인 (무변경) → 점수·등급·근거
```

- 쓰기: 문서 텍스트에서 생성 / 말하기: 문서 속 이미지 **파싱**(생성 금지) 기반 — 후순위
- 기존 확정 문항(writing_v0)은 기본 세트로 병행

## 완료된 것 (트랙 A 성과)

- 채점 파이프라인 + 가드 4종 (테스트 111개)
- **품질 수치 3종**: 재현성 100% (9답안×3회) · 오류 탐지율 90% (20건 중 18건) · 평균 11초
- 문항 5종 main 머지 + 백엔드 DB 시드 등록
- PC+터널 배포 (0원), Dockerfile, CI
- **Gemini 결제 완료** ($300 크레딧 90일) — 쿼터 제약 소멸

---

## 트랙 A 잔여 (연동 마무리)

- [ ] **A-1 서버 재기동** — 세션 재시작으로 죽어 있음. `서버시작.bat` → 새 터널 주소를 백엔드에 전달
- [ ] **A-2 백엔드 연동 E2E** — 백엔드 stub 교체 후 쓰기 채점 왕복. 선행: 재조립 공식·reference_keywords 숨김·Score jsonb 메시지 전달
- [x] **A-3 API 키 인증** (8/1 완료) — `KTEST_API_KEY` 를 넣으면 모든 POST 가 `X-API-Key` 를 요구한다. 안 넣으면 지금처럼 열려 있어 연동 테스트가 막히지 않는다
- [ ] A-4 시연 직전 Cloud Run 이전 판단 (8/8) — 집 PC 리스크(윈도우 업데이트 재부팅·인터넷 장애) 제거
- [ ] A-5 (사소) 재현성 R08 1건 재측정

## 트랙 B 일정 (확정)

### 8/1 — B1 타당성 실험 + 설계
- [x] KOSHA 문서 확보 (화학설비 정비·보수 지침 C-2-2025, 57쪽. 포털이 스크립트 벽이라 직링크 1개 확보)
- [x] 문서 → 쓰기 문항 생성 실험 — 3문항 생성·검증 통과 (2회 실행)
- [x] 인용 검증 실측 — 규칙 없는 프롬프트는 폐기율 100%, 인용 규칙 추가 후 0%. 검증기가 실제로 지어낸 형식을 걸러냄
- [x] `k-test-design` 설계: 생성 프롬프트 구조 / 승인 상태 모델 / 생성 API 계약 → `DESIGN_generate.md`
- [ ] 백엔드·프론트에 분담 안 전달: 문서 업로드 저장(백), 생성 요청·승인 API(백), 관리자 화면(프론트)

**완료 기준**: 실제 KOSHA 문서에서 나온 문항 예시 + 폐기율 수치. 팀에 "됩니다" 증명.

### 8/2 ~ 8/3 — B2 생성 모듈 v0
- [x] `POST /generate-items` — 문서 텍스트 입력 → 쓰기 문항 초안 출력. `POST /verify-items`(재검증)도 함께
- [x] 인용 검증 통합: 관문 5개(G1~G5). 근거 없는 문항은 응답에 실리지 않고 폐기 수·사유만 나간다
- [x] 생성 재현성 실측 — 한 실행 안 3회는 문구까지 같았고, 실행을 나누면 달랐다. 그래서 `wording_reproducible=false` 는 그대로 둔다(약속할 수 없는 것을 약속하지 않는다)
- [x] pytest 추가 (가짜 클라이언트, 네트워크 없이) — 생성 38개 + 인증 9개, 전체 158개 통과

**완료 기준**: 테스트 통과 + 같은 문서 3회 생성 비교 + `/docs`에 엔드포인트 노출.

### 8/4 — 연동의 날 (A와 B 합류)
- [ ] A-2 쓰기 E2E (백엔드 준비되는 대로)
- [x] A-3 API 키 인증 추가 (8/1 에 먼저 끝냄)
- [ ] 생성 API 계약을 백엔드에 전달 (업로드→생성→승인 흐름 합의)

### 8/5 ~ 8/6 — B3 시연 재료
- [ ] 가상 공장 문서 자작 ("(주)K-테스트 식품공장 안전수칙") — KOSHA 내용 기반
- [ ] 디자이너(양은희)에게 요청: 가상 공장 배치도 1장 + 상황 일러스트 2~3장
- [ ] 시연 시나리오 대본: 업로드 → 생성 → 승인 → 응시 → 채점 (승인 화면이 늦으면 "생성 결과 JSON 보여주기"로 대체 가능하게 이중 준비)
- [ ] 생성 품질 점검: 가상 문서로 생성 → 사람 눈 검수 → 문항 품질 문제 목록화

### 8/7 — 말하기 경로 + 여유분 (→ 8/2 에 앞당겨 완료)
- [x] 말하기 경로 확정: **Gemini 받아쓰기(STT)로 오늘 개통** (8/2). `audio: {url}` → 받아쓰기 → 기존 채점 관통, 테스트 190개. Azure 는 계정 나오는 대로 `src/speech/` 포트에 갈아 끼움(발음 채점 이때 추가, API 형식 불변)
- [x] 말하기 문항 5종 초안 (`items/speaking_v0.json`) + 변별력 실측 (완전 82.9 vs 결손 43.3)
- [ ] Azure 발급 후: `AzureStt` 구현 + 발음(delivery) 영역 비중 부여
- [ ] (여유 시) B4 이미지 파싱 설계 — PDF에서 이미지+캡션 추출 검토

### 8/8 ~ 8/9 — 리허설 + 버퍼
- [ ] 전체 흐름 E2E 리허설 (생성부터 최종 등급까지)
- [ ] A-4 Cloud Run 이전 판단·실행
- [ ] HTML 보고서·노션 갱신 (품질 수치 + 생성 데모 반영)

### 8/10 — 1단계 마감 점검

**1단계 완료 기준 (8/10)**
- [ ] 쓰기: 배포 + 백엔드 연동 (검증 수치는 확보됨)
- [ ] 생성: 문서→쓰기 문항 생성이 실제로 돌고 폐기율·재현성 수치 확보
- [ ] 시연 재료: 가상 공장 문서 + 시나리오 대본
- [ ] 말하기: 경로 확정 + 이미지 문항 설계안
- [ ] 테스트 전부 통과 유지

## 궁극 목표 — [난이도: 불가능] 도전 (8/1 선언, 8/2 갱신)

목표: **좁힌 타겟(외국인 노동자 직무 한국어 쓰기·말하기 자동채점)에서 원천 기술(생 LLM 채점) 대비 정량 우위 → 최종적으로 기계-인간 일치율 ≥ 인간-인간 일치율.**

- [x] 계단 1 (7/30~31): 싼 모델이 높임법 오류를 놓침 → 오류 탐지만 상위 모델로 분리 → **탐지율 90%** (20건 중 18)
- [x] 계단 2 (8/1): 생성 문항의 인용이 전부 가짜 조합(폐기율 100%) → "이어진 한 덩어리, 생략부호 금지" 규칙 → **폐기율 0%**
- [x] 계단 3 (7/30): 영어·스팸·베끼기 답안이 등급을 받음 → 유효성 가드 4종 → **전부 무효 처리, 정상 답안 오탐 0**
- [x] 계단 4 (8/1 쓰기, 8/2 말하기 확장): 같은 Gemini 로 생 LLM 채점 vs 우리 구조 대결. 쓰기: 점수 흔들림·만점 남발·악성답안 등급 발급 vs 우리 변동 0. **말하기: 생 LLM 은 무음에 95점 A(근거까지 지어냄)·영어 발화에 95점 A, 우리는 차단·무효** (`outputs/stair4_speaking_20260802.json`)
- [x] 계단 4.5 (8/2): 같은 공격을 우리에게도 쏴서 구멍 발견(무음 환각→78점 B) → 소리 크기(RMS) 관문으로 수리, 테스트 202개 — 남 때린 무기로 자기도 점검한 기록
- [ ] 계단 5 (2단계): 학습 결합모델(XGBoost)로 인간 일치율 향상. 라벨 전략(8/4 갱신):
  **말하기는 AI Hub 사람 점수(내용·언어·전달력 0~5, 수천 건)를 그대로 재사용** —
  쌍대비교 불필요. 쓰기(직무 문항)만 자체 라벨링 100건(앵커 5개 + 절대 등급,
  쌍대비교는 라벨러 일치율이 낮을 때만 예비 투입 — XGBoost 는 rank:pairwise 로 둘 다 지원)
- [ ] 계단 6 (2단계): 인간-인간 일치율을 같은 데이터에서 측정 → **기계-인간 QWK ≥ 인간-인간 QWK** — 이 니치의 SOTA 주장 근거 (통계 설계는 `RESEARCH.md`)
- [ ] (선택·조건부) Whisper LoRA: AI Hub 외국인 발화(음성+정답 전사)로 받아쓰기 학습 → 기본 Whisper·Gemini 와 CER 3자 비교. **착수 조건**: AI Hub 데이터 승인·품질 확인 + 계단 5·6 이 8/13까지 궤도에 오름. 채점 LLM 파인튜닝은 하지 않는다(재현성 무기 훼손)

## 기술 스택 통합 계획 (8/3 확정) — STT · 채점 · XGBoost 3층

```
음성 ─[1층 STT: 오류 보존 LoRA]→ 전사 ─[2층: 구조 분리 채점]→ 자질 ─[3층: XGBoost 결합]→ 점수
```

층마다 이기는 상대와 지표를 고정한다 (비공개 평가셋과의 비교 금지 — RESEARCH.md 주장 규칙):
- 1층: 기본 Whisper·Gemini 상대, 오류 보존율·CER (AI Hub 정답 전사로)
- 2층: 생 LLM 직접 채점 상대 — **완승 실측 완료** (계단 4)
- 3층: 손 가중치 상대, 사람 라벨 QWK 개선 (계단 5)
- 끝판: 음성→최종점수 하류 QWK ≥ 인간-인간 (계단 6) — "받아쓰기가 채점 정확도로
  이어지는지는 아무도 안 쟀다. 우리가 그 지표를 정의하고 전 층에서 베이스라인을 이겼다"

**병목은 라벨.** XGBoost 는 라벨 없이 못 배우므로 라벨링 킷(앵커 5 + 시트 + 지침)을
1단계(8/5~6)로 앞당기고, 8/7~9 에 파일럿 20건으로 프로토콜을 검증한 뒤 2단계에서 100건.

### 기함 선언 (8/3) — "채점을 위한 받아쓰기"

임팩트·논문 요건을 모두 만족하는 단일 기함. 주장 한 줄: **"상용 STT 는 응시자의
실수를 지워 말하기 채점을 오염시킨다. 우리는 실수를 보존하는 모델을 학습했고,
받아쓰기 정확도가 아니라 채점 정확도(사람 점수와의 QWK)로 증명했다."**

구성 4개 (상세·선행 대조는 RESEARCH.md ⑨·⑨-보완·주장 규칙 절):
1. 새 지표 — 오류 보존율 / 2. 새 장치 — 이원 전사 diff(상용의 교정 자리 = 오류 후보)
3. 우리 모델 — Whisper LoRA (+모어별 어댑터) / 4. 새 평가 — 하류 QWK (기존 논문 미측정)
안전장치: LoRA 가 실패해도 "상용 STT 세탁 편향 정량화"만으로 논문 반쪽 성립.
XGBoost(계단 5·6)는 예정대로 가되 논문 주인공은 기함이다. 증류(LLM→소형 모델)는
비전 슬라이드 + 2호 논문 후보로 보류.

### 논문 정면승부 3판 (8/3 추가) — "같은 시험지, 같은 지표"에서 최신 논문의 숫자를 넘는다

"우리만의 새 지표"(하류 QWK)에 더해, **그 논문이 쓴 바로 그 지표**로도 이긴 숫자를 만든다.
전부 공개 데이터라 비교가 성립하고, 전부 2025~2026 최신 논문이다:

| 판 | 상대 (최신 논문) | 시험지 (공개) | 그 논문의 지표 | 우리 무기 |
|---|---|---|---|---|
| 1판 | 2026 Frontiers (NPCR+KLUE-RoBERTa) | AI Hub 에세이 평가 (545) | **QWK** | 자질+XGBoost 3층 |
| 2판 | 2025 MDPI 쌍둥이 (유사도 하이브리드) | 국립국어원 학습자 말뭉치 | **급 예측 정확도·상관** | 체크리스트 방식 (완수 측정) |
| 3판 | 2025~26 GEC 논문들 (K-NCT·KoLLA v2.0) | 공개 오류 교정 시험지 | **F1** | 오류 탐지 모듈 (2층) |

전제: 데이터 신청(545 + 학습자 말뭉치), 상대 논문의 분할·전처리와 동일 조건 확인(다르면 비교 무효),
2판은 쌍둥이 논문 PDF 로 정확한 수치 입수 후 확정. 배치는 2단계, 우선순위 1판 → 2판 → 3판.
정직 규칙: 지면 진 대로 보고하고 원인 분석을 계단으로 만든다 — 이긴 판만 골라 말하지 않는다.

| 날짜 | 할 것 |
|---|---|
| 8/3 | 서버 재기동+주소 전달 · ⑨ Whisper 파일럿(CPU 무료) · ⑩ 드리프트 스냅샷 · AI Hub 확인 |
| 8/4 | 백엔드 E2E (쓰기·말하기 실연동) · ⑤ 메타모픽 스위트 |
| 8/5~6 | **라벨링 킷** · B3 시연 재료 · (AI Hub 오면) CER·세탁 측정 |
| 8/7~9 | 라벨링 파일럿 20건 · 전체 리허설 · Cloud Run 판단 |
| 8/11~ | 라벨링 100건 → XGBoost(계단5) → QWK(계단6) → **정면승부 1판(에세이 QWK)** → LoRA(1층) → 정면승부 2·3판 |

## 방법론 로드맵 (8/2 확정 — 상세·신규성 근거는 `RESEARCH.md`)

후보 11종(논문 ①~④ / 검증·공정성 ⑤~⑧ / LoRA 트랙 ⑨~⑪) + 채점 개선 2종(루브릭 자기교정, 불확실성 선별). 실행 순서:

**즉시 (~8/10, 1단계와 병행)**
- [ ] ⑩ 드리프트 첫 스냅샷 — 고정 음성 세트를 박제하고 Gemini 전사 기록 시작 (비용 0)
- [ ] ⑨ 파일럿 — 기본 Whisper(학습 전)의 오류 보존 성향을 CPU 로 실측, Gemini 와 diff 신호 검증
- [ ] ⑤ 메타모픽 채점 검사 스위트 — 본선 시연에서 즉석으로 돌릴 수 있는 물건
- [ ] AI Hub 승인 나는 즉시: CER 실측(도구 완성됨) + ① 세탁 편향 측정 착수

**2단계 (8/11~, 본진 = 계단 5·6)**
- [ ] 라벨링 100건 (쓰기 50+말하기 50, 2인 독립+3인 조정, 앵커 5개 훈련) → QWK 부트스트랩
- [ ] 루브릭 자기교정 루프 (어긋남 사례로 체크리스트 다듬기)
- [ ] 불확실성 선별 채점 (자동확정 vs 인간검토 곡선)
- [ ] ⑦ 모어별 공정성 감사 (AI Hub 메타데이터)

**조건부 (본진 궤도 확인 후)**
- [ ] ⑨ LoRA 학습 (이원 전사 + diff 오류 후보 아키텍처로) / ⑪ 무음 명시 학습
- [ ] ⑥ 전사 불확실성 전파 (Azure 도입 후) / ⑧ IRT 캘리브레이션 (여유)

**논문 트랙**: 본선 후 HCLT 2026 에 후보 ②(적대적 강건성, 실측 확보됨) 단편 → 2단계 데이터 완성 후 BEA 2027/SLaTE 에 ①·④. HCLT 공고 확인 급함.
- 주의: "한국어 채점 전체 SOTA" 같은 광의 주장 금지 — 벤치마크 질문에 무너진다. 주장은 항상 측정 범위와 함께.

## 의존성

| 항목 | 누구 | 막히면 |
|---|---|---|
| 백엔드 stub 교체·업로드/승인 API | 백예나 | E2E·승인 흐름이 밀림 → 시연은 생성 JSON 직접 보여주기로 대체 |
| 관리자 화면 | 프론트(한효주) | 시연 대본을 승인 화면 없는 버전으로도 준비 (이중 대비) |
| 디자이너 배치도·일러스트 | 양은희 | KOSHA 공개 일러스트로 대체 |
| Azure 계정 | 팀/운영진 | 말하기는 Whisper 경로 확정 |
| ~~Gemini 쿼터~~ | — | **해소됨** (결제 + $300 크레딧) |

## 작업 규칙 (불변)

1. 코드 작업은 `develop` 스킬 → `k-test-dev` 위임
2. 생성 설계 판단은 `k-test-design`/`scoring-design`, 문항은 `item-design`
3. 완료 보고는 실행 결과로 — 수치 없이 "됐다" 금지
4. `schema.py` 추가만 / 응시자별 실시간 생성 금지 / 인용 근거 없는 생성물 금지
5. 매 답변 워크플로우 표시, 작업 후 HISTORY.md 기록
