"""LLM(Gemini) 호출과 인용 검증.

이 프로젝트에서 LLM은 점수를 매기지 않는다. 하는 일은 세 가지뿐이다.
1) 체크리스트 항목의 충족 여부를 0/1로 판정
2) 오류 자질(조사·어미 활용·어휘 오용·높임법, 쓰기는 맞춤법) 추출
3) STT(음성을 글자로 옮기는 기계) 전사본 보정 (transcript.py)

1)과 2)는 답안 원문에서 근거 문장을 그대로 인용하게 하고,
원문에 없는 인용은 citation.py 가 걸러서 버린다.

3)은 점수를 만들지 않고 '무엇을 보고 채점할지'만 정한다.
보정본은 내용·과제 수행에만 쓰이고 문법·어휘는 언제나 원문으로 채점하며,
무엇이 무엇으로 바뀌었는지(diff)는 코드가 직접 대조해서 근거로 남긴다.
"""
